# Camera Api

#### Please make sure you have the next packages globally installed (with NPM):

- nodemon
- yarn

`npm install -g nodemon yarn`

#### Now you can run:

`yarn install`

#### And finally:

`npm start`
